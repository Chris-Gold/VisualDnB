(function($) {
  $(function() { //on DOM ready
      // SIMPLY SCROLL
        $(".scroller").simplyScroll();
      // SLIDER PARALLAX
        $('#da-slider').cslider({
					bgincrement	: 0
				});
  });
})(jQuery);